// import { isNum } from "/utils/asserts"
// import { $BasicOpr, $PLUS } from "./operator*"

// declare global {
// 	interface Number extends $BasicOpr<any> { }
// }

// Number.prototype.$plus = function $plus<X>(n: $PLUS<X>) {
// 	if (isNum(n)) { return this.valueOf() + n }
// 	return n.$plus(this.valueOf() as $PLUS<X>)
// }

// Number.prototype.$neg = function () { return -this.valueOf() }
